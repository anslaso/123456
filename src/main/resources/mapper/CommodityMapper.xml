<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anslaso.ssm.mapper.CommodityMapper">
  <select id="selectCount" resultType="int">
    SELECT COUNT(*) FROM commodity
  </select>

  <select id="selectPage" resultType="com.anslaso.ssm.entity.Commodity" parameterType="map">
    SELECT id, name, price, stock, description
    FROM commodity
    ORDER BY id
    LIMIT #{offset}, #{limit}
  </select>

  <select id="selectById" parameterType="int" resultType="com.anslaso.ssm.entity.Commodity">
    SELECT id, name, price, stock, description FROM commodity WHERE id = #{id}
  </select>

  <update id="updateStock" parameterType="map">
    UPDATE commodity SET stock = stock - #{quantity} WHERE id = #{id} AND stock >= #{quantity}
  </update>

  <update id="setStock" parameterType="map">
    UPDATE commodity SET stock = #{stock} WHERE id = #{id}
  </update>
</mapper>
