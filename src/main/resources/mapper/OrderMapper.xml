<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anslaso.ssm.mapper.OrderMapper">
  <insert id="insertOrder" parameterType="com.anslaso.ssm.entity.Order" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO order_table (user_id, commodity_id, quantity, create_time)
    VALUES (#{userId}, #{commodityId}, #{quantity}, #{createTime})
  </insert>

  <select id="selectByUserId" parameterType="int" resultType="com.anslaso.ssm.entity.Order">
    SELECT id, user_id as userId, commodity_id as commodityId, quantity, create_time as createTime
    FROM order_table
    WHERE user_id = #{userId}
    ORDER BY create_time DESC
  </select>

  <delete id="deleteById" parameterType="int">
    DELETE FROM order_table WHERE id = #{id}
  </delete>
</mapper>
